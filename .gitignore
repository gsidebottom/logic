target/
.idea/
Cargo.lock
.claude/
.pids
.logs
nested-boxes/node_modules/
nested-boxes/dist/

# Python venv created by `./setup.sh` (uv-managed).  `pyproject.toml`
# + `uv.lock` describe the env; `.venv` itself is regenerated
# per-machine via `uv sync`.
.venv/
__pycache__/
*.pyc

# OpenEvolve per-run artifacts (candidate sources, prompts, scores,
# LiteLLM proxy log).  Often hundreds of MB per run; reproducible
# from config.yaml + initial_program.rs.
evo/evolve/runs/

# Phase-1 harvested phase dataset (derived; rebuild via neural/build_dataset.py)
neural/data/phase_ds*/
neural/data/synth_cnf/
matmul/challenges/
matmul/inst/
matmul/yalsat-bin
matmul/dbcache/
# Third-party + downloaded reference material (not ours to commit):
# Perminov's repo clone (~3 GB, has its own .git), fetched papers/pages.
# The committed, verified bits live in matmul/perminov_cache/bits/ and
# the verifier scripts are tracked; everything else here is external.
matmul/perminov_cache/repo/
matmul/perminov_cache/*.html
matmul/perminov_cache/*.pdf
matmul/perminov_cache/*.log
# Regenerable hunt corpora + run logs + derived intermediates.
# Reproduce via the tracked tools/drivers (floors, zrescore, orbitscan,
# orbit55, lift, the *.sh campaign drivers); results are summarized in
# committed CSVs (db_floor_census.csv, f27_campaign_results.csv) and
# canonical representatives are saved under matmul/external/.
matmul/foundR/
matmul/found4r/
matmul/liftedR/
matmul/found/*-orbitbest.bits
matmul/found55/scan/
matmul/found55/cands*/
matmul/found55/ccells/
matmul/found55/fat/
matmul/found55/f27run/
matmul/found55/f26/
matmul/found55/satcomp/
matmul/found55/sq54/
matmul/found55/sq54f27/
matmul/found55/*.log
matmul/found55/*-best.bits
matmul/found55/smoke.bits
matmul/found55/f27names.txt
matmul/found55/f28names.txt
matmul/found55/f28list.txt

# Lean/Lake build + fetched Mathlib (7GB, regenerable via `lake exe cache get`)
# — the project also carries its own proof/.gitignore with /.lake
proof/.lake/
matmul/found55/hunt54/
matmul/found55/cfloor/
matmul/dps48/*.log
zenodo-upload/
matmul/dps48/*.slp
# Kaporin's artifacts (his own public share; provenance in NOTES.md —
# we archive locally but do not redistribute)
matmul/dps48/kaporin/*.for
matmul/dps48/kaporin/*.pdf
matmul/dps48/kaporin/*.json
matmul/found48q/
matmul/dps48/graph48.json
matmul/fringe_exhaustive.log
matmul/chase.log
matmul/found48q_p4.log
matmul/chase2.log
# flip23 storm raw output (distilled artifacts live in matmul/new23/)
matmul/found23q/
# dps48 experiment intermediates (regenerable via committed scripts)
matmul/dps48/accurate/
matmul/dps48/best329/
matmul/dps48/converged/
matmul/dps48/converged2k/
matmul/dps48/mm55_sms/
matmul/dps48/ours_sms/
# external tool builds (PLinOpt + givaro/fflas/linbox stack)
matmul/dps48/build/
matmul/dps48/plinopt/
matmul/dps48/stack/
matmul/found23p/
matmul/found48p/
matmul/dps48/orbit/
matmul/dps48/orbit_perm/

# binary SR certificates (tens of MB; regenerable with sat -b hydra_satsuma --proof)
matmul/r22/*.sr
matmul/r22/subgame_3x3_17.json
matmul/r22/subgame_3x3_ab_koszul.json
matmul/r22/subgame_3x3_18.json
