target/
.idea/
Cargo.lock
.claude/
.pids
.logs
nested-boxes/node_modules/
nested-boxes/dist/

# Python venv created by `./setup.sh` (uv-managed).  `pyproject.toml`
# + `uv.lock` describe the env; `.venv` itself is regenerated
# per-machine via `uv sync`.
.venv/
__pycache__/
*.pyc
